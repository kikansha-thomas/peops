# peops office プロジェクト設定

## GitHub自動バックアップルール

このプロジェクト（peops office）はGitHubリポジトリ `kikansha-thomas/peops`（Public）と連携しています。

**作業を依頼され、その変更が正常に完了した場合、以下を自動で実行すること:**

1. 変更内容を確認（`git status` / `git diff`）
2. push前に機密情報が含まれていないか確認する
   - パスワード、APIキー、アクセストークン、シークレットキーなどが含まれていないか
   - 疑わしい内容があれば、pushせずユーザーに報告する
3. 適切な内容のコミットメッセージで `git add` → `git commit`
4. `git push` して `kikansha-thomas/peops` の `main` ブランチに反映

**pushしてはいけないケース:**
- 作業がまだ途中の場合
- エラーが発生していて未解決の場合
- ユーザーから明確な完了確認が得られていない場合
- 機密情報が含まれている可能性がある場合（まずユーザーに確認する）

このルールが適用されるのは、明確に完了した作業のみ。中途半端な状態やデバッグ中のコードをpushしないこと。

## 絶対にGitHubに含めてはいけないもの

以下は `.gitignore` で除外済み。新しいファイルを追加する際も、これらのパターンに該当するものはコミットしないこと:

- `node_modules/`
- `請求書管理アプリ/data/app.db`（SQLiteデータベース、取引先の実データを含む）
- `.DS_Store`
- `.claude/settings.local.json`（ローカル権限設定、ローカル環境のファイルパス情報を含む）
- パスワード、APIキー、アクセストークンなどの機密情報を含むファイル・記述

新しい機密性の高いファイル（`.env`、認証情報、証明書など）を作成する場合は、必ず `.gitignore` に追加してからコミットすること。

## リポジトリ情報

- GitHub: https://github.com/kikansha-thomas/peops
- 可視性: Public
- デフォルトブランチ: main
